version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "chmod +x start.sh && ./start.sh"

workflows:
  say-hello-workflow:
    jobs:
      - say-hello